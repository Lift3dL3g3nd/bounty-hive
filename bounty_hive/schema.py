from __future__ import annotations

# Increment ONLY when the serialized schema changes
NORMALIZED_POLICY_SCHEMA_VERSION = 1

# Supported versions for loading (forward-compat guard)
SUPPORTED_NORMALIZED_POLICY_VERSIONS = {1}
